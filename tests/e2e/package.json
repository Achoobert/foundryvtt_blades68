{
  "name": "blades68-e2e-tests",
  "version": "0.0.0",
  "description": "Cypress harness that drives a running Foundry VTT instance to execute the Blades68 Quench batches.",
  "private": true,
  "type": "module",
  "scripts": {
    "tests": "cypress open",
    "tests:ci": "cypress run --headless --browser chrome --config-file cypress.config.ci.js"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "devDependencies": {
    "cypress": "^15.17.0",
    "cypress-if": "^1.13.2"
  }
}
