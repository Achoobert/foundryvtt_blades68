console.log(
  'Quench batches live under tests/quench and run inside a live Foundry world.\n' +
    'Start the foundry_14 container against the blades68-dev world, open the Quench panel, and run "All".\n' +
    'For a scripted/headless run use `npm run test:e2e` (Cypress) from tests/e2e.'
);
